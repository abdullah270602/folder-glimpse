<Window x:Class="FolderGlimpse.Preview.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FolderGlimpse" Width="430" Height="200" MinHeight="150" MaxHeight="620"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        ShowActivated="False" Topmost="True" Background="#F7F7F7"
        SnapsToDevicePixels="True" UseLayoutRounding="True" PreviewKeyDown="WindowPreviewKeyDown">
  <Window.Resources>
    <SolidColorBrush x:Key="PanelBrush" Color="#F7F7F7" />
    <SolidColorBrush x:Key="TextBrush" Color="#171717" />
    <SolidColorBrush x:Key="SubtleTextBrush" Color="#666666" />
    <SolidColorBrush x:Key="LineBrush" Color="#18000000" />
    <SolidColorBrush x:Key="ScrollTrackBrush" Color="#0D000000" />
    <SolidColorBrush x:Key="ScrollThumbBrush" Color="#59000000" />
    <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#80000000" />
    <SolidColorBrush x:Key="AccentBrush" Color="#026AFB" />
    <SolidColorBrush x:Key="SelectionBrush" Color="#26026AFB" />
    <SolidColorBrush x:Key="SelectionBorderBrush" Color="#026AFB" />
    <Style TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource TextBrush}" /></Style>
    <Style x:Key="RowSelectionCheckStyle" TargetType="CheckBox">
      <Setter Property="Width" Value="18" /><Setter Property="Height" Value="18" /><Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="CheckBox">
        <Border x:Name="Box" Width="16" Height="16" CornerRadius="3" Background="Transparent" BorderBrush="{DynamicResource SubtleTextBrush}" BorderThickness="1">
          <Path x:Name="Tick" Visibility="Collapsed" Width="10" Height="8" HorizontalAlignment="Center" VerticalAlignment="Center"
                Data="M 1,4 L 4,7 L 9,1" Stretch="Fill" Stroke="{DynamicResource AccentBrush}" StrokeThickness="2"
                StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
        </Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsChecked" Value="True"><Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /><Setter TargetName="Box" Property="BorderThickness" Value="1.5" /><Setter TargetName="Tick" Property="Visibility" Value="Visible" /></Trigger>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource AccentBrush}" /></Trigger>
          <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Box" Property="BorderThickness" Value="2" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="EntryContextMenuStyle" TargetType="ContextMenu">
      <Setter Property="Background" Value="{DynamicResource ControlBrush}" /><Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource LineBrush}" /><Setter Property="BorderThickness" Value="1" /><Setter Property="Padding" Value="4" />
    </Style>
    <Style x:Key="EntryMenuItemStyle" TargetType="MenuItem">
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" /><Setter Property="Padding" Value="13,7" /><Setter Property="MinWidth" Value="180" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="MenuItem">
        <Border x:Name="Surface" Background="Transparent" CornerRadius="5" Padding="{TemplateBinding Padding}"><ContentPresenter ContentSource="Header" /></Border>
        <ControlTemplate.Triggers><Trigger Property="IsHighlighted" Value="True"><Setter TargetName="Surface" Property="Background" Value="{DynamicResource ControlHoverBrush}" /></Trigger></ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
  </Window.Resources>
  <Border x:Name="RootBorder" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource LineBrush}"
          BorderThickness="1" CornerRadius="10">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="1" />
        <RowDefinition Height="*" />
        <RowDefinition Height="1" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="1" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Grid x:Name="HeaderPanel" Grid.Row="0" Margin="18,15,18,13">
        <Grid.ColumnDefinitions><ColumnDefinition Width="34" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
        <TextBlock Text="📁" FontSize="22" VerticalAlignment="Center" />
        <StackPanel Grid.Column="1">
          <TextBlock Text="{Binding FolderName}" FontSize="15" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
          <TextBlock x:Name="FolderPathText" Text="{Binding FolderPath}" ToolTip="{Binding FolderPath}" Visibility="{Binding PathVisibility}"
                     FontSize="11" Foreground="{DynamicResource SubtleTextBrush}" Margin="0,2,0,0" TextTrimming="CharacterEllipsis" />
        </StackPanel>
      </Grid>
      <Border x:Name="HeaderDivider" Grid.Row="1" Background="{DynamicResource LineBrush}" />
      <Grid Grid.Row="2">
        <ListBox x:Name="EntryList" ItemsSource="{Binding Entries}" Background="Transparent" BorderThickness="0" Padding="8,7" SelectionMode="Extended"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                 PreviewMouseLeftButtonDown="EntryListMouseLeftButtonDown"
                 PreviewMouseRightButtonDown="EntryListMouseRightButtonDown">
          <ListBox.ItemContainerStyle>
            <Style TargetType="ListBoxItem">
              <Setter Property="Focusable" Value="True" /><Setter Property="IsHitTestVisible" Value="True" />
              <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=OneWay}" />
              <Setter Property="Height" Value="{Binding Tag, RelativeSource={RelativeSource AncestorType=ListBox}}" />
              <Setter Property="Padding" Value="9,0" /><Setter Property="HorizontalContentAlignment" Value="Stretch" />
              <Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="ListBoxItem">
                  <Border x:Name="RowSurface" Margin="1" Padding="{TemplateBinding Padding}" CornerRadius="5" BorderThickness="1" BorderBrush="Transparent">
                    <ContentPresenter />
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="RowSurface" Property="Background" Value="{DynamicResource ControlHoverBrush}" /></Trigger>
                    <Trigger Property="IsSelected" Value="True"><Setter TargetName="RowSurface" Property="Background" Value="{DynamicResource SelectionBrush}" /></Trigger>
                    <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="RowSurface" Property="BorderBrush" Value="{DynamicResource SelectionBorderBrush}" /></Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate></Setter.Value>
              </Setter>
            </Style>
          </ListBox.ItemContainerStyle>
          <ListBox.ItemTemplate>
            <DataTemplate>
              <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition x:Name="CheckColumn" Width="0" /><ColumnDefinition x:Name="IconColumn" Width="28" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <CheckBox x:Name="SelectionCheck" Visibility="Collapsed" Style="{StaticResource RowSelectionCheckStyle}" IsChecked="{Binding IsSelected, Mode=OneWay}" Click="SelectionCheckboxClicked" VerticalAlignment="Center" />
                <Image x:Name="EntryIcon" Grid.Column="1" Source="{Binding Icon}" Width="18" Height="18" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding Name}" ToolTip="{Binding Name}" FontSize="13" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextBrush}" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="3" Text="{Binding SizeText}" Visibility="{Binding SizeVisibility}" FontSize="11" Margin="12,0,3,0" VerticalAlignment="Center"
                           Foreground="{DynamicResource SubtleTextBrush}" />
                <TextBlock Grid.Column="4" Text="{Binding ModifiedText}" Visibility="{Binding ModifiedVisibility}" FontSize="11" Margin="12,0,3,0" VerticalAlignment="Center"
                           Foreground="{DynamicResource SubtleTextBrush}" />
              </Grid>
              <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding DataContext.ShowCheckboxes, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                  <Setter TargetName="CheckColumn" Property="Width" Value="25" /><Setter TargetName="SelectionCheck" Property="Visibility" Value="Visible" />
                </DataTrigger>
                <DataTrigger Binding="{Binding DataContext.ShowEntryIcons, RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
                  <Setter TargetName="IconColumn" Property="Width" Value="0" /><Setter TargetName="EntryIcon" Property="Visibility" Value="Collapsed" />
                </DataTrigger>
              </DataTemplate.Triggers>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding EmptyVisibility}">
          <TextBlock Text="This folder is empty" FontSize="13" Foreground="{DynamicResource SubtleTextBrush}" />
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding LoadingVisibility}">
          <ProgressBar IsIndeterminate="True" Width="110" Height="3" />
          <TextBlock Text="Loading…" FontSize="12" Margin="0,10,0,0" HorizontalAlignment="Center" Foreground="{DynamicResource SubtleTextBrush}" />
        </StackPanel>
        <StackPanel x:Name="ErrorPanel" MaxWidth="300" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding ErrorVisibility}">
          <TextBlock Text="Unable to read this folder" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Center" />
          <TextBlock Text="{Binding ErrorMessage}" FontSize="11" Margin="0,5,0,0" TextAlignment="Center" TextWrapping="Wrap"
                     Foreground="{DynamicResource SubtleTextBrush}" />
        </StackPanel>
      </Grid>
      <Border Grid.Row="3" Background="{DynamicResource LineBrush}" />
      <Grid Grid.Row="4" Margin="18,8" Visibility="{Binding ActionBarVisibility}">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
        <TextBlock Text="{Binding SelectionStatus}" VerticalAlignment="Center" FontSize="12" Foreground="{DynamicResource SubtleTextBrush}" />
        <Button Grid.Column="1" Content="Open" MinHeight="30" Padding="14,4" Click="OpenSelectedClicked" />
      </Grid>
      <Border x:Name="FooterDivider" Grid.Row="5" Background="{DynamicResource LineBrush}" Visibility="{Binding FooterVisibility}" />
      <TextBlock x:Name="FooterText" Grid.Row="6" Text="{Binding Status}" Visibility="{Binding FooterVisibility}" FontSize="11" Margin="18,10"
                 Foreground="{DynamicResource SubtleTextBrush}" />
    </Grid>
  </Border>
</Window>
